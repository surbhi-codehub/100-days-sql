
# 🌸 100 Days SQL Journey
## Day 35

## 🌼 Today's Topic
**AVG() Function**

**Goal:** Learn how to calculate the average value of numeric data using `AVG()`.

---

## 💖 A Little Message for Today
Every query you complete is another step toward becoming confident with SQL.
Stay patient, stay curious, and let your progress build day by day.

---

## 📚 Quick Revision
The `AVG()` function returns the average value of a numeric column.
You can combine it with `WHERE` to calculate averages for specific records.

Example:
```sql
SELECT AVG(salary)
FROM employees;
```

---

# 🎯 Today's SQL Missions

### Mission 1 (Very Easy)
Find the average salary of all employees.

### Mission 2 (Easy)
Find the average price of all products.

### Mission 3 (Easy+)
Find the average quantity ordered in the `orders` table.

### Mission 4 (Medium)
Find the average salary of employees whose `department_id` is 2.

### Mission 5 (Challenge)
Find the average price of products in the **Electronics** category.

---

## 🏆 Bonus Challenge
Find the average stock quantity of products whose stock quantity is greater than 50.

---

## 💡 SQL Tip of the Day
`AVG()` ignores `NULL` values automatically, so only non-NULL values are included in the calculation.

---

## 🌱 Tomorrow's Preview
Tomorrow you'll learn how to identify the smallest value using the `MIN()` function.

---

## 📝 Git Commit Message
`Completed Day 35 SQL Mission`

---

## 🎉 Closing Note
Great work reaching another milestone! Keep practising regularly, and every new function you learn will make SQL feel more natural.
